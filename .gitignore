# Ignore user config and secrets
.env
.env.secrets
jstack.config
site-templates/*/.env
nginx/ssl/*.key
nginx/certbot/conf/*
nginx/htpasswd

# Claude Code configuration (machine-specific)
.claude/
CLAUDE.md

# Service data and credentials
data/
nginx/htpasswd

# Supabase edge functions (separate repo)
supabase/

# Custom apps (separate repos)
apps/

# Server-specific docker compose overrides
docker-compose.override.yml

# Ignore generated nginx configs (created dynamically by install script)
nginx/conf.d/*.conf
nginx/certbot/

# claude
.claude/
CLAUDE.md

# Node.js artifacts
node_modules/
dist/
*.log

# External repositories (cloned during installation)
n8n-mcp-proxy/

# Docker artifacts
**/docker-volumes/
**/backup/
**/.docker/
docker-compose.override.yml

# System files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/

# Build/test output
coverage/
*.out

# Ignore generated sites and runtime/output
data/
sites/
logs/
backups/
tests/unit/
supabase/
